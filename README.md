# Bills Manager
A simple application to manage and track bills using a terminal interface and a web-based UI.


## Features
- Add, edit, and delete bills
- View bills via terminal or web interface
- Search and filter bills by date, amount, or category


## Installation
1. Clone the repository:
   ```sh
   git clone https://github.com/your-username/bills-manager.git



cd bills-manager


pip install -r requirements.txt
npm install  # If the web app uses JavaScript


## Usage

### Terminal Application
Run the terminal app with:
```sh
python main.py


flask run


Open http://localhost:5000 in your browser.

## Configuration
Rename `.env.example` to `.env` and update values as needed.
